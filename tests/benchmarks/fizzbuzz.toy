for (var i = 1; i <= 10000; i++) {
	var result: String = "";

	if (i % 3 == 0) result = result .. "fizz";
	if (i % 5 == 0) result = result .. "buzz";

	//finally
	if (result != "")
		print result;
	else
		print i;
}
