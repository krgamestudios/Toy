fn makeCounter() {
	var counter: Int = 0;

	fn increment() {
		return ++counter;
	}

	return increment;
}

//'tally' becomes a closure
var tally = makeCounter();

for (var result = 0; result < 10; result = tally()) {
	print result;
}
